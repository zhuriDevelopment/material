spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: hadoop
    url: jdbc:mysql://127.0.0.1:3306/zhuriMaterial?useSSL=false
  application:
    name: MaterialManagement-Facade
  servlet:
    multipart:
      # 针对post请求数据量可能过大的情况
      maxFileSize: 100Mb
      maxRequestSize: 100Mb